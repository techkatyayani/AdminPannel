import 'package:flutter/material.dart';

class SimilarProductIdScreen extends StatelessWidget {
  const SimilarProductIdScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
